# hdfs-manager-gui-app
I have built a gui app that manages operations on hdfs like creating a directory, you can put files into hdfs using a simple gui buttons, you can also download files from hdfs to local
